pragma solidity 0.7.6;

contract RewardPool {
  function startStaking(address staker, uint amount, uint stakingStart) external {
    
  }
  
  function completeStaking(address staker, uint stakingEnd) external {
    
  }
      
  function payReward(address staker) external {
    
  }
}